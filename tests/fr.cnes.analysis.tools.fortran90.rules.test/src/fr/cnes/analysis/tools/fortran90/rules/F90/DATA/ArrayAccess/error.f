program ESSAI

  implicit none

  integer, dimension(4) :: u
  integer, dimension(4) :: v

! initialisation du contenu du tableau u a partir de l'index v
  v = (/ 1, 2, 2, 3 /)
  u(V) = (/ 1, 2, 3, 4 /)

! resultats obtenus sur ortie standard
  print*, '-- Tableau v --', v
  print*, '-- Tableau u --', u

end program ESSAI
